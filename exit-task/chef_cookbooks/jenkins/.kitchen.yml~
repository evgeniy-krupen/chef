---
driver:
  name: vagrant

provisioner:
  name: chef_solo
  client_rb:
      audit_mode: :audit_only

platforms:
  - name: sbeliakou/centos-6.7-x86_64

suites:
  - name: default
    run_list:
      - recipe[java::default]
    attributes:
